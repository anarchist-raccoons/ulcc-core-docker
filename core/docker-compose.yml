version: '3.3'

volumes:
  ulcccore:

networks:
  internal:

services:

  ulcccore: 
    build: 
      context: .
      args:
         BRANCH: ${BRANCH}
         APACHE_RUN_USER: ${APACHE_RUN_USER}
         APACHE_RUN_GROUP: ${APACHE_RUN_GROUP}
    env_file:
      - .env
    volumes:
      - ulcccore:$APP_WORKDIR/archives
    networks:
      internal:
    expose: 
      - 80
    ports:
      - 80:80